#include <stdio.h>
#include <math.h>
int main(){
    int x = 3;
    int r =sqrt(x),i;
    printf("%d",r);
    for(i=2;i<=sqrt(x);i++){
        printf("%d,",i);
        printf("!%d!",r);
        fabs(1);
    }
}
